---
title: Loading
description: Les Loadings servent à indiquer le chargement d'un élément d'interface. Ils peuvent être accompagnés d'un label pour préciser le caractère des données chargées.
---

# Design

## Règles d'usage

### Positionnement

#### Chargement

Les indicateurs de progressions sont toujours centrés dans les zones respectives du *layout,* correspondantes aux parties importantes de l’interface.

#### Mise à jour

Les indicateurs sont positionnés par dessus les zones mises à jour, centrés et par dessus un *overlay* blanc semi-transparent.

### Cycle de vie

#### Chargement

Le chargement des composants se fait en deux étapes :

1. Le *layout* est chargé, et on distingue (si possible) les différentes parties de l’interface et dans chaque zones on place un loading rond, petit et indéterminé. La zone la plus importante de l’interface se voit dotée d’un indicateur plus grand que les autres.
2. Les composants finaux apparaissent dans l’interface dès qu’ils sont disponibles (et donc pas forcément au même moment).

#### Chargement enrichi

Si possible, la zone la plus importante de l’interface verra son indicateur rond remplacé par un *skeleton*. Le reste du processus de chargement restant inchangé.

#### Mise à jour

Pour éviter les effets de flash pendant les mises à jour, on va forcer la visibilité des indicateurs pendant au minimum 500 milli-secondes avant de disparaitre via un *fade*. Les apparitions de l’*overlay* et de l’indicateur sont par contre immédiates.

# Angular

## Loading Angular `v20.1`

Component selector : `lu-loading`

```ts
import { LoadingComponent } from '@lucca-front/ng/loading';
```

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-loaders-loading-angular-basic--basic)

# HTML

## Loading HTML

```css
@forward '@lucca-front/scss/src/components/loading';
```

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-loaders-loading-html-css-basic--basic)

<callout background="1">

La classe `mod-fullPage` permettent de centrer un loading dans une page entière.

Les classes `mod-popin` et `mod-drawer` permettent de centrer un loading dans une fenêtre de dialogue en version classique ou drawer.

</callout>
