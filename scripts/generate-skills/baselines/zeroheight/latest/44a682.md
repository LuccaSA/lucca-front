---
title: Resource card  🧪
description: Le composant Resource card permet d’afficher des ressources sous forme de liste, empilée ou en grille. Chaque Resource card est interactive et permet à l’utilisateur d’accéder à un contenu plus détaillé de cette ressource.
---

# Angular

## Resource card Angular `v21.0`

Mots-clés : carte, fiche, tuile, ressource

Component selector : `lu-resource-card`

```ts
import { ResourceCardButtonComponent, ResourceCardComponent, ResourceCardLinkComponent, ResourceCardWrapperComponent } from '@lucca-front/ng/resource-card';
```

<tabs>
<tab>

<tab-title>**Base**</tab-title>

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-structure-resource-card-angular-basic--basic)

</tab>
<tab>

<tab-title>**Drag and drop**</tab-title>

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-structure-resource-card-angular-drag-and-drop--basic)

</tab>
</tabs>

---

# Changelog

## Resource card changelog

### 21.3.0

#### Added

- `RESOURCE_CARD_HEADING_LEVEL` and `RESOURCE_CARD_SIZE` constants, together with the `ResourceCardHeadingLevel` and `ResourceCardSize` types, are now publicly exported and used to type the `headingLevel` and `size` inputs.

### 21.1.4

#### Fixed

- Misspelled host attribute of the card.

### 21.1.3

#### Fixed

- Action rendering with the `critical` palette (`mod-critical`).

### 21.0.0

#### Added

- `lu-resource-card` component (`resourceCard`) with the `size`, `headingLevel`, `grid`, `draggable` and `disabled` inputs.
- `lu-resource-card-wrapper` component to lay out a collection of cards, along with the `LU_RESOURCE_CARD_WRAPPER_INSTANCE` injection token.
- `[luResourceCardAction]` directive for `a` and `button` elements, to turn the whole card into an actionable element.
