---
title: Container
description: Les Containers servent à structurer et centrer le contenu d'une page. Ils apportent des marges, s'adaptent à la largeur de la page et à la présence ou non du menu principal.
---

# Angular

## Container Angular `v20.3`

Mots-clés : conteneur, wrapper

Component selector : `lu-container`

```ts
import { ContainerComponent } from '@lucca-front/ng/container';
```

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-structure-container-angular-basic--basic)

# HTML

## Container HTML

Classe CSS : `.container`

```css
@forward '@lucca-front/scss/src/components/container';
```

[Basic](https://lucca-front.lucca.io/master/storybook/iframe.html?id=documentation-structure-container-html-css-basic--basic)

# Changelog

## Container changelog

### 21.3.0

#### Added

- `CONTAINER_SIZE` constant and `ContainerSize` type are now publicly exported and used to type the `max` input.

### 21.2.0

#### Fixed

- Default `display` no longer overrides the layout of the container's content.
- `justify-content` is no longer forced by the container.

### 20.3.0

#### Added

- `lu-container` component (`container`) with the `center`, `max` and `overflow` inputs.

### 20.1.0

#### Added

- Container support inside `pageHeader` and `footer` to align their content with the page container.
