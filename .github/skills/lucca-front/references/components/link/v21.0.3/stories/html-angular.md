# link — Angular _(HTML/CSS)_

```css
@forward '@lucca-front/scss/src/components/link';
```

```html
Angular Navigation side:
<a luLink="${…}" ${…}${…}${…}>${…}</a>
<br />
Browser Navigation side:
<a href="${…}" luLink${…}${…}${…}>${…}</a>
```
