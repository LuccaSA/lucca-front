### 22.0.0

#### Removed

- `grey` and `primary` values of the `palette` input — use `neutral` and `product` instead.

### 21.3.0

#### Added

- `lu-highlight-text` component (`highlightText`) with the `palette` input, to emphasize a piece of text inside a paragraph.
