### 21.2.3

#### Added

- `lu-software-icon-wrapper` component (`softwareIconWrapper`) to display a collection of software icons, with the `size`, `max` and `intl` inputs.
- `[SoftwareIconWrapperItem]` directive to declare each icon of the collection.
