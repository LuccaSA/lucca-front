### 21.3.0

#### Added

- `lu-impersonation` component (`impersonation`) to let an administrator browse the application as another user.
- `selectedUser` model, plus the `enableFormerEmployees`, `clear` and `intl` inputs.
