{
	"name": "lucca-front-intellisense",
	"displayName": "Lucca Front IntelliSense",
	"description": "Autocomplete, hover and diagnostics for @lucca-front/scss custom properties and pr-u-* utility classes",
	"version": "0.3.1",
	"publisher": "lucca",
	"private": true,
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/LuccaSA/lucca-front.git",
		"directory": "packages/vscode-extension"
	},
	"engines": {
		"vscode": "^1.90.0"
	},
	"categories": ["Programming Languages", "Linters"],
	"main": "./dist/extension.js",
	"activationEvents": ["onLanguage:css", "onLanguage:scss", "onLanguage:less", "onLanguage:html", "onLanguage:typescript"],
	"contributes": {
		"commands": [
			{
				"command": "luccaFront.reloadManifest",
				"title": "Lucca Front: Reload CSS API manifest"
			},
			{
				"command": "luccaFront.showProblems",
				"title": "Lucca Front: Show problems"
			}
		],
		"configuration": {
			"title": "Lucca Front IntelliSense",
			"properties": {
				"luccaFront.diagnostics.enabled": {
					"type": "boolean",
					"default": true,
					"description": "Report deprecated tokens/classes and unknown pr-u-* classes as diagnostics."
				},
				"luccaFront.diagnostics.deprecatedSeverity": {
					"type": "string",
					"enum": ["error", "warning", "information", "hint"],
					"default": "warning",
					"description": "Severity for deprecated custom properties and utility classes."
				},
				"luccaFront.diagnostics.unknownClassSeverity": {
					"type": "string",
					"enum": ["error", "warning", "information", "hint"],
					"default": "warning",
					"description": "Severity for pr-u-* classes not present in the installed @lucca-front/scss version."
				},
				"luccaFront.manifestPath": {
					"type": "string",
					"default": "",
					"scope": "machine-overridable",
					"description": "Absolute path to a manifest.json, overriding node_modules discovery (for library development/testing)."
				},
				"luccaFront.experimental.cssDeprecations": {
					"type": "boolean",
					"default": false,
					"markdownDescription": "**Experimental.** Flag deprecated custom properties and `pr-u-*` utility classes (diagnostics, hover notices, completion tags). Off by default because deprecation metadata in `@lucca-front/scss` is still incomplete."
				},
				"luccaFront.docs.storybookBaseUrl": {
					"type": "string",
					"default": "",
					"markdownDescription": "Base URL of a Storybook deployment. When set, utility-class hovers include a deep link to the matching Storybook docs page. Use a fixed URL (e.g. `https://<host>/main/storybook`), or include the `{version}` placeholder to target the versioned deployment matching the installed package — e.g. `https://lucca-front.lucca.io/{version}/storybook` becomes `.../v21.0/storybook` (minor-level; patch is dropped). Tokens and unmapped utilities always link to the Prisme design-system site instead."
				}
			}
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"watch": "tsc -p tsconfig.build.json --watch",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "jest --config jest.config.js",
		"vsix": "npm run build && vsce package --no-dependencies"
	},
	"devDependencies": {
		"@types/vscode": "1.90.0",
		"@vscode/vsce": "^3.3.0"
	}
}
